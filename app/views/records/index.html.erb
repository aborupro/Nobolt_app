<% provide(:title, '完登記録(全ユーザ)') %>

  <div class="row login_home">
    <div class="col-md-6">
      <h3>完登記録(全ユーザ)</h3>
      <% if @records.any? %>
        <%= will_paginate @records%>

        <ol class="records">
          <%= render @records %>
        </ol>

        <%= will_paginate @records%>
      <% end %>

    </div>
  </div>