- provide(:title, 'ジム選択')
.container
  h1.text-center
    | ジム選択
  = render 'select'
  p.text-center
    | 新しくジムを登録する場合はこちら⇨
    = link_to "新規ジム登録", new_gym_path
  = will_paginate @gyms
  ul.gym_list_box
    = render @gyms
  = will_paginate @gyms
